# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Environments
.venv/
venv/
ENV/
env/

# Databricks / Spark temporary run files
derby.log
metastore_db/
spark-warehouse/
/lake/
/tmp/
/reports/

# OS generated files
.DS_Store
.DS_Store?
ehthumbs.db
Thumbs.db

.databricks

# Local LangGraph checkpoint DBs (runtime state, not source)
checkpoint*.db
checkpoint*.db-wal
checkpoint*.db-shm
downloaded_checkpoint*
*.db-wal

# Editor / OS artifacts
.vscode/
.fuse_hidden*

# Local raw source data staged for pipeline testing (not source code)
/Source/
